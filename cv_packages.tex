\usepackage[hidelinks]{hyperref}
\usepackage{etoolbox}
\usepackage{enumitem}

\usepackage[T1]{fontenc}
\usepackage{newtxtext,newtxmath}

% For citation formatting
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
% \usepackage{hyperref}

% --- BibLaTeX Configuration ---
\usepackage[
    backend=biber,
    style=authoryear,
    sorting=ydnt, % Sort by Year (Descending), Name, Title
    maxbibnames=99, % List all authors
    dashed=false, % Do not use dashes for recurring authors
    doi=true,
    url=true
]{biblatex}

% --- Custom Field Formatting ---
% 1. Link formatting
\DeclareFieldFormat{preprint}{\par\small\textit{Preprint: \url{#1}}}
\DeclareFieldFormat{code_repo}{\par\small\textit{Code: \url{#1}}}

% 2. Add custom fields to the end of the entry
\AtEveryBibitem{
  \clearlist{language} % Cleanup
  \csappto{blx@bbx@note}{%
    \printfield{preprint}%
    \printfield{code_repo}%
    \iffieldequalstr{cover_article}{true}{\par\textbf{(Cover Article)}}{}%
  }
}


% --- Bold Your Name (Optional but recommended) ---
\usepackage{xpatch}
\xpatchbibmacro{name:family-given}
  {\ifgiveninits}
  {\ifdefstrequal{#1}{Dunn}{\mkbibbold{#1}}{#1}\ifgiveninits}
  {}{}

\addbibresource{publications.bib}

% Set your name here
\def\name{Dr. Casey William Dunn}

% Replace this with a link to your CV if you like, or set it empty
% (as in \def\footerlink{}) to remove the link in the footer:
\def\footerlink{}

% PDF metadata
\hypersetup{
  colorlinks = true,
  urlcolor = black,
  pdfauthor = {\name},
  pdfkeywords = {evolution, invertebrate zoology, computational biology},
  pdftitle = {\name: Curriculum Vitae},
  pdfsubject = {Curriculum Vitae},
  pdfpagemode = UseNone
}

\geometry{
  body={6.5in, 8.5in},
  left=1.0in,
  top=1.25in
}

% Customize page headers
\pagestyle{myheadings}
\markright{\name}
\thispagestyle{empty}

% Custom section fonts with rules
\usepackage{sectsty}
\sectionfont{\rmfamily\mdseries\Large}
\subsectionfont{\rmfamily\mdseries\itshape\large}

% Add horizontal rules under section headings
\let\oldsection\section
\renewcommand{\section}[1]{\oldsection{#1}\vspace{-0.5em}\rule{\textwidth}{0.4pt}\vspace{0.5em}}

% Don't indent paragraphs.
\setlength\parindent{0em}

% Make lists without bullets
\renewenvironment{itemize}{
  \begin{list}{}{
    \setlength{\leftmargin}{1.5em}
  }
}{
  \end{list}
}


\newcommand{\entrylabel}[1]{\mbox{{\rm #1}}\hfill}
\newenvironment{entry}[1]
{\begin{list}{}%
{\renewcommand{\makelabel} {\entrylabel}%
\setlength{\labelwidth}{#1}%
\setlength{\leftmargin}{#1}%
\addtolength{\leftmargin}{2mm}
\setlength{\parsep}{0pt}
}%
}%
{\end{list}}

% Toggle for short CV variant
\newtoggle{shortcv}
